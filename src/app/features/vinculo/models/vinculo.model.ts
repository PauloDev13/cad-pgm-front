import {
  NomeableRequest,
  NomeableResponse,
} from '../../../shared/model/generic/base-generic.model';

export type VinculoRequestDTO = NomeableRequest;
export type VinculoResponseDTO = NomeableResponse;
