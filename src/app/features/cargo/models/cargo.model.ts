import {
  NomeableRequest,
  NomeableResponse,
} from '../../../shared/model/generic/base-generic.model';

export type CargoRequestDTO = NomeableRequest;
export type CargoResponseDTO = NomeableResponse;
