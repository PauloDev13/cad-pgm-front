import {
  NomeableRequest,
  NomeableResponse,
} from '../../../shared/model/generic/base-generic.model';

export type SistemaRequestDTO = NomeableRequest;
export type SistemaResponseDTO = NomeableResponse;
